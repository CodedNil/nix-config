if [[ "$1" == "--position" ]]; then
    playerctl metadata --format '{{ position }}' | awk '{ printf("%.6f\n", $1 / 1000000) }'
elif [[ "$1" == "--length" ]]; then
    playerctl metadata --format '{{ mpris:length }}' | awk '{ printf("%.6f\n", $1 / 1000000) }'
fi